## Naturalness Score

**5/5 — Native-like**

Several grammar slips (agreement, verb form, double negative), but the underlying phrasing is ordinary native technical English; errors do not affect the score and 0 Medium/High Observations remain. Polish is optional.

## Errors

### 1. ⚠️ Error

```diff
- a user click the button
+ a user clicks the button
```

Subject-verb agreement; singular `a user` takes `clicks`.

### 2. ⚠️ Error

```diff
- the request are send
+ the request is sent
```

Singular `the request` takes `is`, and the passive requires the past participle `sent`.

### 3. ⚠️ Error

```diff
- without no delay
+ without any delay
```

Double negative; `without` plus `no` is ungrammatical. Use `without any delay`.

## Observations

No observations. The phrasing is already native-like.

## Revised Sentence

When a user clicks the button, the request is sent to the server without any delay.

## Register

Neutral (technical)
