A verified answer requires retrieval I cannot perform in this environment (no access to AWS documentation fetchers, context7, or web search).

Based on training data, not freshly verified: Amazon Bedrock AgentCore is an AWS offering announced as part of the Amazon Bedrock generative-AI platform, intended to provide infrastructure and runtime services for building, deploying, and operating AI agents at production scale. It is positioned to handle concerns such as agent runtime/hosting, memory, identity, tool/gateway integration, and observability so developers do not have to assemble those pieces themselves, and it is framework-agnostic (usable with agents built on frameworks like LangGraph, CrewAI, Strands, etc.) rather than being tied to a single agent SDK. Details such as GA status, exact component names, pricing, and supported models change frequently and may be out of date.

To confirm, consult the official AWS Bedrock AgentCore documentation at https://docs.aws.amazon.com/bedrock-agentcore/ and the AWS What's New / Bedrock product page. The preferred specialized tool for this lookup would be the context7 MCP server (resolve-library-id then query-docs) targeting the AWS Bedrock AgentCore documentation.
