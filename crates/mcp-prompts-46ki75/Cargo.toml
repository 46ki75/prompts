[package]
name = "mcp-prompts-46ki75"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
publish = false
description = "MCP server exposing the prompts published to GitHub Pages as MCP resources."

[lints]
workspace = true

[dependencies]
rmcp = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
reqwest = { workspace = true }
axum = { workspace = true }
clap = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

[dev-dependencies]
rmcp = { workspace = true, features = ["client"] }
wiremock = { workspace = true }

# Binary names (unlike package names) may start with a digit, so the
# installed command can match the GitHub `46ki75/prompts` identity.
[[bin]]
name = "46ki75-prompts"
path = "src/bin/main.rs"
